services:
  - type: worker
    name: slopslotbot-worker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python SlopSlotBot.py
    envVars:
      - key: BOT_TOKEN
        sync: false